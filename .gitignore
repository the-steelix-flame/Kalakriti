# ── dependencies ───────────────────────────────────────────────────────────
node_modules/
.venv/
__pycache__/
*.pyc

# ── secrets: never commit these ────────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
jwt_secret.txt
passport_key.pem

# ── generated native project (recreate with `npx expo prebuild`) ───────────
app/android/
app/ios/

# ── build output ───────────────────────────────────────────────────────────
.expo/
dist/
web-build/
*.apk
*.aab
build/
.gradle/

# ── runtime data ───────────────────────────────────────────────────────────
backend/*.db
backend/media/
*.log

# ── editor / OS ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# Rows removed by dedupe_listings.py, kept so a deletion is reversible. Real
# listing data from whoever ran it, and of no use to anybody else.
backend/dedupe-backups/

# JSON snapshots of rows cleanup_listings.py removed. Real listing data,
# kept locally so a wrong call can be undone, and not ours to publish.
backend/cleanup-backups/
