# Every dependency is open source. Nothing here bills anyone.
fastapi==0.115.6           # MIT
uvicorn[standard]==0.34.0  # BSD
pydantic==2.10.4           # MIT
python-dotenv==1.0.1       # BSD
openai==1.59.6             # MIT - client only; points at NVIDIA's OpenAI-compatible endpoint
pillow==11.1.0             # HPND
cryptography==44.0.0       # Apache-2.0 - Ed25519 passport signing
rembg==2.0.61              # MIT - U^2-Net background matting
onnxruntime==1.20.1        # MIT - CPU inference for rembg
rapidocr-onnxruntime==1.4.4  # Apache-2.0 - local OCR (detection + recognition)
sqlalchemy==2.0.36           # MIT - listings, publications, orders, event log
requests==2.32.3             # Apache-2.0 - marketplace + payment HTTP

# ── hosting ────────────────────────────────────────────────────────────────
# Only needed when this runs somewhere other than a laptop. Both are optional at
# import time: the app falls back to SQLite and local disk if they are absent.
psycopg[binary]==3.2.3       # LGPL-3.0 - Postgres driver, for DATABASE_URL
boto3==1.35.90               # Apache-2.0 - S3-compatible object storage (R2, Supabase)
PyJWT==2.10.1